{
  "name": "guard",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "bootstrap": "bash scripts/install-linux.sh",
    "install:all": "npm --prefix contracts install && npm --prefix web install",
    "test": "npm --prefix contracts test && npm --prefix web test",
    "dev": "bash scripts/dev.sh",
    "build:web": "npm --prefix web run build",
    "acceptance:multipass": "bash scripts/multipass-acceptance.sh"
  }
}
