{
  "name": "chat-app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm --prefix contracts install && npm --prefix orchestrator install && npm --prefix web install",
    "test": "npm --prefix contracts test",
    "dev": "bash scripts/dev.sh"
  }
}
